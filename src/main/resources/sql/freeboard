CREATE TABLE pet_free_board (
    regNum NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    p_Name VARCHAR2(100) NOT NULL,
    p_guardian VARCHAR2(100) NOT NULL,
    title VARCHAR2(100) NOT NULL,
    p_content CLOB NOT NULL,
    p_pwd VARCHAR2(18) NOT NULL,
    regDate DATE DEFAULT SYSDATE NOT NULL,
    viewCnt NUMBER DEFAULT 0 NOT NULL
);

drop table pet_free_board;

select * from PET_FREE_BOARD;
-- 테이블 설명 추가
COMMENT ON TABLE pet_free_board IS '반려동물 자유게시판';

-- 각 컬럼 설명 추가
COMMENT ON COLUMN pet_free_board.regNum IS '등록 순번 (자동 증가 PK)';
COMMENT ON COLUMN pet_free_board.p_Name IS '반려동물 이름';
COMMENT ON COLUMN pet_free_board.p_guardian IS '보호자 이름 (작성자)';
COMMENT ON COLUMN pet_free_board.title IS '게시글 내용';
COMMENT ON COLUMN pet_free_board.p_content IS '게시글 내용';
COMMENT ON COLUMN pet_free_board.p_pwd IS '비밀번호';
COMMENT ON COLUMN pet_free_board.regDate IS '게시글 등록일시';
COMMENT ON COLUMN pet_free_board.viewCnt IS '조회수';

insert into pet_free_board(p_Name,p_guardian,title,p_content,p_pwd)
values('송이','홍길동','고양이가 새벽내내 울어요','새벽3시부터 6시까지
목놓아 울고 식사도 거르고 있습니다.','1234');

insert into pet_free_board(p_Name,p_guardian,title,p_content,p_pwd)
values('삼이','동길','눈이 충혈','눈이 붉어지고
 눈물이 납니다.','1234');
 commit;